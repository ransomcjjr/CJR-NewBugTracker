@using CJR_NewBugTracker.Models
@model LoginViewModel
@{
    ViewBag.Title = "DemoLogin";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0" />
    <meta name="format-detection" content="telephone=no">
    <meta charset="UTF-8">
    <meta name="description" content="Violate Responsive Admin Template">
    <meta name="keywords" content="Super Admin, Admin, Template, Bootstrap">
    <title>RFP Bug Tracker</title>

    <!-- CSS -->
    <link href="~/css/bootstrap.min.css" rel="stylesheet">
    <link href="~/css/style.css" rel="stylesheet">
    <link href="~/css/generics.css" rel="stylesheet">
</head>

<body class="skin-blur-violate text-center">
    <div class="text-center">
        <h1>Demo Login</h1>
        <div row>
            <div class="col-md-12">
                <div id="changediv">
                <h3>Take a scroll through the bug tracker software! Click on one of the roles below.</h3>
            </div>
            <div>&emsp;</div>
            </div>
        </div>
        <img class="img-rounded" src="~/img/demobackgroundcropped.jpg" />
    <div>&emsp;</div>
    <div class="row">
        <div class="col-md-12">
            <div class="col-md-3 inlineblock">
            @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(m => m.Email, new { @Value = "rfpadmin@mailinator.com" })
                @Html.HiddenFor(m => m.Password, new { @Value = "Admin001!" })
                <input type="submit" value="Admin Demo" class="btn btn-default btn-danger img-rounded img-shadowed" />
            }
        </div>
            <div class="col-md-3 inlineblock">
                @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
                    @Html.AntiForgeryToken()
                    @Html.HiddenFor(m => m.Email, new { @Value = "rfppm@mailinator.com" })
                    @Html.HiddenFor(m => m.Password, new { @Value = "PMdemo001!" })
                    <input type="submit" value="Project Manager Demo" class="btn btn-default btn-warning img-rounded img-shadowed" />
                }
            </div>
            <div class="col-md-3 inlineblock">
    @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(m => m.Email, new { @Value = "rfpdev@mailinator.com" })
        @Html.HiddenFor(m => m.Password, new { @Value = "Devdemo001!" })
        <input type="submit" value="Developer Demo" class="btn btn-default btn-danger img-rounded img-shadowed" />
    }
</div>

            <div class="col-md-3 inlineblock">
    @using (Html.BeginForm("Login", "Account", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
        @Html.AntiForgeryToken()
        @Html.HiddenFor(m => m.Email, new { @Value = "rfpsubmitter@mailinator.com" })
        @Html.HiddenFor(m => m.Password, new { @Value = "Subdemo001!" })
        <input type="submit" value="Submitter Demo" class="btn btn-default btn-warning img-rounded img-shadowed" />
    }
</div>

            </div>
    </div>
    </div>
    <div>&emsp;</div>
    <div>
        <a href="~/Docs/BugTrackerPermissions.pdf" target="_blank">|View Role Permissions|</a>
        <a href="~/Docs/SRS - (MVC) BugTracker without Diagram.pdf" target="_blank">View Project Specs|</a>
    </div>
</body>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}

